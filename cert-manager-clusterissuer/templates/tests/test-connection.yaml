apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cert-manager-clusterissuer.fullname" . }}-test-connection"
  labels:
    {{- include "cert-manager-clusterissuer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cert-manager-clusterissuer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
