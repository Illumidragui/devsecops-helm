apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hello-world.fullname" . }}-html
  labels:
    {{- include "hello-world.labels" . | nindent 4 }}
data:
  index.html: |
    {{- .Values.html | nindent 4 }}
