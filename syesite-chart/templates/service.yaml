apiVersion: v1
kind: Service
metadata:
  name: {{ include "syesite-chart.fullname" . }}
  labels:
    {{- include "syesite-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syesite-chart.selectorLabels" . | nindent 4 }}
